// Native side registers @CapacitorPlugin(name = "NotificationSound").
// The web app reaches it via Capacitor.Plugins.NotificationSound — no JS
// wrapper needed, so this entry point is intentionally empty.
module.exports = {};
